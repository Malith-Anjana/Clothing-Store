import React from "react";
import { useLocation } from "react-router-dom";

const Success = () => {
  return (
    <div style={{ justifyContent: "center", alignItems: "center", display: "flex" }}>
      <h1 style={{color:'green', fontSize:'100px'}}>Success</h1>
    </div>
  );
};

export default Success;
